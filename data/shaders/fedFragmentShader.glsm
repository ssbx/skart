#version 120

// Ouput data
//out vec3 color;
varying vec3 fragmentColor;

void main()
{

	// Output color = red, full opacity

	gl_FragColor = vec4(fragmentColor * fragmentColor, 1);

}
