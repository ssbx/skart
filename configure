#!/bin/sh

# update dependencies
git submodule init
git submodule update

# cleanup build dir if exist
rm -rf build
mkdir  build && 

# create Unix Makefiles project
cd build && cmake ..

echo "\nYou can now run \"cd build && make\"\n"

